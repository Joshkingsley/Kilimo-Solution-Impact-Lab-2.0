# Secrets & environment
.env
.env.*
!.env.example
*.pem
.dev.vars

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Node / Workers
node_modules/
dist/
.wrangler/

# Corpus raw snapshots are committed deliberately; scratch downloads are not
corpus/tmp/

# OS / editor
.DS_Store

# Local-only presentation material (never committed)
docs/local/

# RAG local index (rebuildable offline from corpus/raw via `python -m app.rag.cli ingest`)
data/*.sqlite3
data/*.sqlite3-wal
data/*.sqlite3-shm
